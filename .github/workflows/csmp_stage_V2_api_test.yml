name: Stage CSMP V2 API Test Job

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: stage

    steps:
    - uses: actions/checkout@v3

    - name: Run CSMP V2 API Tests
      run: |
        echo "Running CSMP V2 API Tests Cases"
        echo "job completed"